#pragma once

// Dont need to make this a property until/unless decide to have tick lists

//class RateLimitedStateBroadcastProperties
//{
    //public:
        //int tick;
        //int interval;   // tick # to broadcast on

    //RateLimitedStateBroadcastProperties()
    //: tick(0), interval(1)
    //{}
//};
