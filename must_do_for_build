Confirm:
redo mouse

Check after code frozen, but before release:
debug symbols are stripped / obfuscated binary
fix valgrind memory errors in client

One last check before release:
production keybindings

running on permaserver:
small script to always run game
ulimit unlimited for core dump generation
copy core w/ timestamp and restart

Linux Build
--Ubuntu 11.10
--Ubuntu 11.10 64bit
--Fedora19 or w/e
--OpenSuse10 or w/e

Put links on site

BRANDON:
map
windows build
truncate hitscan laser

NEXT BUILD:
spawner location selection
fixed block selector
skeleton / model
agent arm / hitscan laser comes out of it


REMOVING SYMBOLS:
strip -g

ALSO (see http://stackoverflow.com/questions/2872638/how-to-remove-strings-from-a-compiled-binary-so)
compile with
    -fvisibility=hidden

and mark cython-imported symbols as:
__attribute__((visibility("default")));

(the complement is)
__attribute__((visibility("hidden")));

dynamic symbol table pruning
